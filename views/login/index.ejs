<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head', { title: 'Đăng nhập' }) %>
  </head>
  <body>
    <div
      class="w-screen h-screen flex items-center justify-end"
      style="
        background-image: url('images/bg-auth.jpg');
        background-size: cover;
        background-position: center;
      "
    >
      <div class="md:w-1/2 w-full flex items-center justify-center px-5">
        <div class="w-full max-w-lg p-8 bg-white rounded-2xl shadow-lg">
          <div class="flex gap-4 justify-center items-center mb-6">
            <div>
              <span
                class="text-purple-600 text-3xl font-extrabold tracking-wider"
                >T1</span
              >
              <span class="text-gray-800 text-xl font-semibold compact"
                >CARE</span
              >
            </div>
            <p>|</p>
            <h2 class="text-2xl font-bold text-center text-gray-800">
              Đăng nhập
            </h2>
          </div>

          <form action="/login" method="POST" class="space-y-5">
            <!-- E-mail -->
            <div>
              <label
                for="phone"
                class="block text-sm font-medium text-gray-700 mb-1"
                >Tài khoản</label
              >
              <input
                type="phone"
                id="phone"
                name="phone"
                value="0876139365"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-500"
              />
            </div>

            <!-- Mật khẩu -->
            <div>
              <label
                for="password"
                class="block text-sm font-medium text-gray-700 mb-1"
                >Mật khẩu</label
              >
              <input
                type="password"
                id="password"
                name="password"
                value="Admin@123"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-500"
              />
            </div>

            <!-- Submit -->
            <button
              type="submit"
              class="w-full bg-purple-600 text-white py-2 rounded-xl hover:bg-purple-700 transition duration-200"
            >
              Đăng nhập
            </button>
          </form>

          <!-- Link đăng ký -->
          <p class="mt-6 text-sm text-center text-gray-600">
            Chưa có tài khoản?
            <a href="/register" class="text-purple-600 hover:underline"
              >Đăng ký</a
            >
          </p>
          <div class="text-center">
            <a
              href="/forgot-password"
              class="text-sm text-purple-600 hover:underline"
              >Quên mật khẩu?</a
            >
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
