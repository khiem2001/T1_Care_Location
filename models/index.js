const UserModel = require('./userModel');
const DeviceModel = require('./deviceModel');
const UserDeviceModel = require('./userDeviceModel');

module.exports = {
  DeviceModel,
  UserDeviceModel,
  UserModel,
};
